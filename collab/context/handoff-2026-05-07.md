# Handoff Опуса — 2026-05-07

> Документ-страховка. Если контекст упал, новая сессия Опуса (или Дашин Клод) — открывает этот файл первым. Цель: за 5 минут подняться в курс продуктовой и тактической ситуации.

---

## Команда и роли

- **Никита** (главный) — продакт+техлид, owner проекта Glava, дает go на всё
- **Даша** — продакт, отвечает за смысл книг и protocol decisions
- **Курсор** — AI, у Никиты, делает реализацию + прогоны на сервере
- **Дашин Клод** — AI, у Даши, делает spec/review/координацию
- **Опус (я)** — AI, отдельно, делает архитектурные ходы, защиты, рефакторы. Связан с командой через Никиту.

Протокол отношений: Дашин Клод и Курсор — основные исполнители. Опуса зовут когда нужен **второй взгляд / архитектурный ход / разбор мутаций регрессий**. Опус коммитит и мерджит сам через `gh` (авторизован).

---

## Где мы по проекту

**Каракулина — пилотный кейс.** Книга-биография через 14-агентный LLM пайплайн. Цель: довести до gate3 (с фото) и потом gate4 (обложка + финал). Сейчас в main: исправления **Stage 2** регрессий, ещё не дошли до полного Stage 1→4 прогона.

**6 регрессий v43:**

| # | Описание | Статус |
|---|---|---|
| #1 | historical_notes 6→0 в PDF | ✅ закрыта (волна 1.1, verified v46) |
| #2 | callouts duplicated между ch_02 и ch_03 | ✅ закрыта (волна 1.1, verified v46) |
| #3 | «огурцы исчезли» через revision | ✅ закрыта структурно (волны 1.2→1.3.2, verified v51 сценарий A) |
| #4 | документы дублированы между ch_02 и ch_04 | ✅ закрыта (волна 1.2, FC v2.9 cross-chapter) |
| #5 | Татьяна missing в bio_data.family | ❌ backlog, не начато |
| #6 | галлюцинированная медаль (FC strength) | ❌ backlog, не начато |

---

## Регрессия #3 — 5 мутаций, 5 защит (ключевая история)

Эпизод об огурцах в Молдавии исчезал из книги через **5 разных механизмов**. Каждый раз Курсор находил новую мутацию через content-verification PDF/book_FINAL, не через формальные метрики.

| Версия | Механизм | Защита (где) |
|---|---|---|
| v43 | GW удалил при revision вместо корректировки даты | anti-deletion в GW v2.15 + post-validator volume-check (волна 1.2.2) |
| v47 | FC поставил `legitimate_deletion=true` без evidence (phantom) | evidence-required в FC v2.10 + `_verify_evidence_in_book` (волна 1.2.3) |
| v48 | FC scope не видел `historical_context` от Историка → Историка-вставки помечались как hallucination | 3-й валидный источник в FC v2.11 + `run_fact_checker(historical_context=...)` (волна 1.3) |
| v49 | FC quote описывал другой эпизод (счётчик) при общей теме (конфликт с зятем) | topic-overlap relative ≥ 25% в FC v2.12 + `_evidence_topic_overlap` (волна 1.3.1) + ВСЕГДА evidence-check независимо от ratio |
| v50 | FC quote с 1 общим токеном («валентина» — имя субъекта) | abs минимум 2 shared tokens в FC v2.13 + Object Markers Test процедура (волна 1.3.2) |

**Стейт защиты на сейчас (после PR #7):**
- Evidence-check ВСЕГДА запускается при `legitimate_deletion=true`, независимо от volume ratio
- Verdict `blocked_phantom_evidence` ловит: quote отсутствует / topic-overlap < 25% / shared_count < 2
- FC v2.13 промпт обязывает FC перечислить 2-4 маркера эпизода и проверить ≥2 в evidence quote перед `legitimate_deletion=true`

**v51 verified (2026-05-07): сценарий A.** FC v2.13 не ставит legitimate_deletion на огурцы. FC iter2 PASS (0 critical / 0 major / 2 warn). Volume iter1: −74 chars, ok_within_threshold. Огурцы в финальной книге, ch_04: «История с огурцами стала семейной легендой. Зять Владимир привёз из Молдавии чемодан огурцов в хорошем чешском чемодане...»

Цепочка мутаций v47→v51 закрыта on-run. Класс «cross-chapter framing_distortion → unauthorized deletion» закрыт через **3-уровневую защиту**: промпт (object_marker rule в FC v2.13) + validator (`_evidence_topic_overlap` ≥ 0.25 + `shared_count` ≥ 2 + `_verify_evidence_in_book`) + post-volume guard.

**Structural alternative (запрет legitimate_deletion для cross-chapter) НЕ нужен** — incremental defenses сработали.

---

## Structural alternative (архивный — не понадобился)

На случай если регрессия #3 вернётся через 6-ю мутацию: запрет `legitimate_deletion` для cross-chapter вообще. Любой framing_distortion → severity=warning, не major. Cross-chapter дубль → человек руками. Сейчас НЕ актуально — incremental defenses держат класс.

---

## Что в main (на 2026-05-07)

```
7c72fb7 Merge PR #7 wave 1.3.2: min_shared_tokens=2 + FC v2.13 object markers
894ad31 Merge PR #6 wave 1.3.1: evidence topic-overlap (closes v49)
e0bf04a Merge PR #5 wave 1.3:   FC v2.11 historical_context (closes v48)
4b6cf04 Merge PR #4 wave 1.2.3: evidence-required (closes v47)
72e959b Merge PR #3 wave 1.2:   cross-chapter symmetry + GW anti-deletion
3ea22a4 Merge PR #2 wave 1.1.6: order_strict per ref-type (closes order ORDER false-positive)
a27761c Merge PR #1 wave 1.1:   ref-architecture for callouts/historical_notes (closes #1, #2)
```

Активные промпты: FC v2.13, GW v2.15, LD v3.22 (subheading + callout/note refs).

---

## Где в коде что искать

| Что | Где |
|---|---|
| Защита от unauthorized deletion | `pipeline_utils.validate_revision_volume()` + helpers `_evidence_topic_overlap`, `_topic_tokens`, `_verify_evidence_in_book` |
| Validator для layout (paragraphs/callouts/historical_notes) | `scripts/validate_layout_fidelity.py` (`REF_TYPE_CONFIG`, параметризован) |
| Resolve text от ref в PDF | `scripts/pdf_renderer.py:BookIndex.get_callout / get_historical_note / .get()` |
| FC принимает historical_context | `pipeline_utils.run_fact_checker(historical_context=...)` |
| Stage 2 prog с интеграцией всех защит | `scripts/test_stage2_pipeline.py` (передаёт historical_context в FC и в revision GW, вызывает validate_revision_volume после revision) |

**Тесты:** `tests/test_revision_volume.py` (25 тестов) + `test_validate_layout_fidelity.py` (24) + `test_pdf_renderer_refs.py` (17) + `test_quality_gates.py` (4) + `test_fact_checker_historical_context.py` (9). Итого **79 тестов**.

**Запуск всех релевантных:**
```bash
pytest tests/test_revision_volume.py tests/test_validate_layout_fidelity.py \
       tests/test_pdf_renderer_refs.py tests/test_quality_gates.py \
       tests/test_fact_checker_historical_context.py -q
```

---

## Дисциплина протокола (что сохранять)

Разработано через серию инцидентов. Если новая сессия Опуса забудет — напомнить:

1. **Артефакт ≠ побочный эффект.** При любом «X сделано» — открыть сам артефакт, не верить proxy-метрикам типа «pytest passed», «exit 0», «PR merged». Зафиксировано в `collab/context/dev-review-protocol.md`.

2. **Posture-forcing observation.** При закрытии задачи — обязательно одно предложение конкретного наблюдения о финальном артефакте: «открыл PDF v46, на стр 5 видно...». Не «PASS, файлы на местах». Если закрывающий не может написать конкретно — задача не закрыта.

3. **Семантическая верификация ≠ формальная.** PASS-verdict с условиями (`ok_with_X`) требует cross-check на смысл условия, не только форму. Третий инцидент того же класса добавлен в protocol после v49.

4. **Defense in depth.** Промпт + код + тест. Промпт ловит большинство, код ловит остаток. Тест документирует ожидание.

5. **Не передавать override без обсуждения.** Override-флаги (`--allow-layout-mismatch`, `--allow-deletion-drop`, `--allow-fc-fail`) — только для аварийной отладки. При FAIL — пауза, отчёт, обсуждение, потом решение. Курсор после v45-инцидента это применяет, нужно не разрушить.

6. **Узкое ТЗ.** Если задача делегируется Курсору — явный список файлов «в scope» и «не трогать». Тогда детектор scope creep становится механическим («тронул ли файл вне списка?»).

---

## Открытые задачи (backlog)

- **Регрессия #5** (Татьяна missing в bio_data.family) — другой класс, не #3. Не начато.
- **Регрессия #6** (галлюцинированная медаль) — FC strength insufficient. Не начато.
- **err_004** (historical_note записан в body нарратива вместо `book.historical_notes[]`) — структурная ошибка GW связи с волной 1.1, отдельная мини-задача.
- **task 030** (`collab/tasks/030-proofreader-script-spec.md`) — Proofreader → скрипт, design spec готов, ждёт ответы Даши+Никиты на 3 вопроса (Languagetool vs pyspellchecker / acceptance criteria / источник style_passport).
- **Holistic скриптование агентов** — после Каракулины. Список из 7 кандидатов (Proofreader Tier-1, QA Layout Tier-1, Layout Designer + Art Director Tier-2, Cleaner / Completeness Auditor / Fact Checker частично — Tier-2/3, Photo Editor — Tier-3). 5 строго остаются агентами.

---

## Полный прогон Каракулиной

**v51 verified (Stage 2). v52 (полный Stage 1→4) — следующий milestone.** План:
1. **v52 — полный Stage 1→4** как milestone «4/6 регрессий v43 закрыто». Бюджет ~$10. Ждём go от Никиты.
2. Закрытие #5 (Татьяна missing) + #6 (галлюцинированная медаль), волна 1.4 → ~3-5 дней работы.
3. Полный v53 как «6/6 закрыто, готово к gate3 (фото)».

---

## Конкретный следующий шаг для новой сессии

**v51 закрыт сценарием A. Сейчас — развилка.** Варианты по приоритету:

1. **v52 полный Stage 1→4** — верификация что 4/6 регрессий действительно держатся в полном прогоне (а не только на Stage 2 фрагменте). Ждём go Никиты на ~$10 бюджет.
2. **Волна 1.4 — регрессии #5 + #6** (Татьяна / медаль). Можно начать параллельно с v52 если Никита даст go: они на других слоях (#5 — bio_data extraction, #6 — FC strength).
3. **err_004** (historical_note в body вместо `book.historical_notes[]`) — мини-задача, можно подцепить к 1.4.
4. **task 030** (Proofreader → скрипт) — design spec ждёт ответы Даши+Никиты на 3 вопроса. Опус не блокирующее звено.

**Дисциплина:** не лезть в код без go от Никиты. Если задача неясна — спросить, не догадываться (применили на FC scope диагностике v48, сработало).

---

## Краткая хронология сессии (для Опуса в новой сессии — что я делал)

1. **Аудит pipeline** на стартовом этапе — нашёл 17 структурных проблем + 23 в DB/Prompts/Config.
2. **Holistic-аудит «какие агенты должны быть скриптами»** — 7 кандидатов, Proofreader top-1.
3. **Волны 1.1 → 1.3.2** — закрытие регрессий v43 #1, #2, #3 (5 мутаций), #4. PR #1 → #7.
4. **5 итераций защит регрессии #3.** Каждая закрывала новый механизм мутации.
5. **Protocol updates** — артефакт vs proxy, семантическая верификация, audit-checkpoint.
6. **v51 verified (2026-05-07, сценарий A)** — Курсор привёл цитату из ch_04, FC iter2 PASS, volume ok_within_threshold. Регрессия #3 структурно закрыта. Защита cross-chapter framing_distortion стоит на трёх уровнях (промпт + validator + post-volume guard).
7. **Сейчас** — handoff обновлён, ждём решения Никиты по v52 / волне 1.4.

---

**Если что-то непонятно — спросить Никиту, не догадываться.** Контекст сложный, мутации регрессий неочевидны, лучше уточнить чем сделать неправильный ход.
