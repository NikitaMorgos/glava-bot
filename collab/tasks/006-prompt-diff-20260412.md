# Diff для Даши/Claude — задача 006

Дата: 2026-04-12  
Назначение: сверка изменений перед деплоем на стороне Даши/Claude.

## 1) `prompts/03_ghostwriter_v2.6.md` -> v2.7

Что добавлено/изменено:

- Обновлена шапка версии:
  - было: `v2.6`
  - стало: `v2.7 (2026-04-12, Dasha+Dev)`
- В блок жёстких ограничений добавлены явные запреты:
  - выдуманные люди/родственники (пример с братьями),
  - выдуманные эпизоды/предметы (пример с пианино/шубой),
  - подмена точной формулировки факта (пример про документы/вокзал),
  - эмоциональные интерпретации без источника (`драма`, `утомил`, т.п.).
- Добавлен отдельный жёсткий блок:
  - запрет утверждать смерть героя при `fact_map.subject.death_year == null`,
  - запрет выводить смерть косвенно из событий.
- В разрешённое поведение добавлено:
  - сохранять колоритные слова рассказчика из source (`выковыривал`, `хлебосольная`).
- Усилена структура глав:
  - в гл.02 запрещены портретные/психологические блоки как основной контент,
  - такие элементы должны быть в гл.03/04.
- Смягчена формулировка структуры гл.03:
  - вместо буквальных меток `ЧАСТЬ 1 / ЧАСТЬ 2` -> `Блок "характер"` и `Блок "ценности и установки"`.
  - добавлен запрет печатать в тексте главы метки `ЧАСТЬ 1 / ЧАСТЬ 2`.
- В гл.04 добавлен явный анти-дубль:
  - запрет повторять эпизоды, уже подробно раскрытые в гл.02/03.

## 2) `prompts/04_fact_checker_v2.md` -> v2.1

Что добавлено/изменено:

- Добавлена шапка версии:
  - `v2.1 (2026-04-12, Dasha+Dev)`.
- Добавлен новый блок:
  - `ОБЯЗАТЕЛЬНЫЕ КРАСНЫЕ ФЛАГИ (не пропускать)`.
- В red flags явно зафиксированы обязательные ошибки:
  - выдуманные родственники/персоны,
  - выдуманные эпизоды/предметы,
  - невозможная хронология (пример с революцией 1917),
  - ложный вывод смерти при `fact_map.subject.death_year == null`,
  - структурные нарушения по главам и дубли между главами.
- Скорректировано правило приоритета источников:
  - было: фактически "протокол важнее карты",
  - стало: базовый источник `fact_map`, транскрипт — для перепроверки цитат/контекста; при критичном расхождении фиксировать ambiguity/error с ссылкой на оба источника.

## Комментарий по совместимости

- Изменения не трогают JSON-схему выхода ни у Писателя, ни у Фактчекера.
- Изменения усиливают только policy-правила и критерии fail/pass.
- Если хотите оставить ownership только за Дашей/Claude: можно перенести эти пункты 1-в-1 в вашу версию и не использовать данный файл напрямую.
